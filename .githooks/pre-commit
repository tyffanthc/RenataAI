#!/usr/bin/env sh
set -eu

echo "[pre-commit] Verifying commit rules acknowledgement..."

if command -v py >/dev/null 2>&1; then
  py tools/commit_rules_guard.py
elif command -v python >/dev/null 2>&1; then
  python tools/commit_rules_guard.py
else
  echo "[pre-commit] ERROR: Python launcher not found (py/python)." >&2
  exit 1
fi

echo "[pre-commit] COMMIT_RULES_GUARD passed."
